DEPOP 027 — PBMC LIBRARY v2.62 GITHUB UPDATE

Adds/updates Depop case 027 using the current PBMC renderer geometry:
- short-edge transaction anchors only
- distributed anchor positions
- maximum two bends per arrow
- perpendicular departure and arrival at every Transaction field

Copy library.json and depop/ into the repository root, preserving paths.
