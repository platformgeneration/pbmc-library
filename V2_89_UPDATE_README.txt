v2.89 — 2026-09-15
- Replaced the About portrait asset with the deterministic 4x upscale/sharpen of the user's original photo (no generative facial changes).
- Portrait is approximately half the previous desktop display width: 105px instead of 210px.
- Portrait moved to the left of the intro copy; intro text stays directly to its right.
- Mobile keeps the same left-photo/right-copy relationship at 82px (74px on very narrow screens).
- PBMC Library/header/menu implementation from v2.88 is unchanged.
